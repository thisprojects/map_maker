import { ObjectWithKeyStrings } from "../types/editor";

export const GRID_SIZE = 50; // Size of each grid cell
export const SCALE_FACTOR = 0.1; // Scale factor to reduce the map size to 1/10th
export const TEXTURE_COLOURS: ObjectWithKeyStrings = {
  brickWall: "#a52a2a",
  concreteFloor: "#cccccc",
  woodFloor: "#d2b48c",
  tileFloor: "#add8e6",
};
